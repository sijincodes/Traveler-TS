export type Navlinks = {
  link: string;
  id: string;
};

export type HeroB = {
  title: string;
  subtitle: string;
  text: string;
  btn1: string;
  btn2: string;
  img: any;
};

export type FooterDetail = {
  titles: {
    title: string;
  }[];
  links: {
    link: string;
  }[][];
  sociallinks: {
    icon: any;
  }[];
};
